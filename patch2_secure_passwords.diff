diff --git a/app/models.py b/app/models.py
--- a/app/models.py
+++ b/app/models.py
@@
 from django.db import models
+from django.contrib.auth.hashers import make_password, identify_hasher

 class Users(models.Model):
     # existing fields...
     username = models.CharField(max_length=150, unique=True)
     password = models.CharField(max_length=255)
     # your other fields...

+    def save(self, *args, **kwargs):
+        """
+        Override save() to hash plaintext passwords automatically using PBKDF2.
+        Skips rehashing if the password is already hashed.
+        """
+        try:
+            identify_hasher(self.password)
+            # Already hashed, do nothing
+        except Exception:
+            # Not hashed yet
+            self.password = make_password(self.password)
+        super().save(*args, **kwargs)
+
diff --git a/app/backends.py b/app/backends.py
--- a/app/backends.py
+++ b/app/backends.py
@@
+from django.contrib.auth.hashers import check_password
+from app.models import Users
+
+class CustomAuthBackend:
+    """
+    Custom authentication backend for the Users model.
+    Allows login using PBKDF2-hashed passwords.
+    """
+    def authenticate(self, request, username=None, password=None, **kwargs):
+        try:
+            user = Users.objects.get(username=username)
+            if check_password(password, user.password):
+                return user
+        except Users.DoesNotExist:
+            return None
+
+    def get_user(self, user_id):
+        try:
+            return Users.objects.get(pk=user_id)
+        except Users.DoesNotExist:
+            return None
diff --git a/app/views.py b/app/views.py
--- a/app/views.py
+++ b/app/views.py
@@
 from django.shortcuts import redirect
 from django.contrib import messages
 from django.contrib.auth.views import LoginView
+from django.contrib.auth.hashers import check_password
 from .models import Users
@@
         if username == 'admin' and password == '@admin':
             return redirect('admin_reports')  # or any page

-        user = authenticate(self.request, username=username, password=password)
-        if user is not None:
+        try:
+            user = Users.objects.get(username=username)
+            if check_password(password, user.password):
+                self.request.session['user_id'] = user.id
+                self.request.session['username'] = user.username
+                self.request.session['state']= user.state
+                self.request.session['city']= user.city
+                self.request.session['station']= user.station
+                self.request.session['rank'] = user.rank
+
+                if not remember_me:
+                    self.request.session.set_expiry(0)
+                else:
+                    self.request.session.set_expiry(1209600)
+
+                rank = getattr(user, 'rank', None)
+                if rank in ['Constable', 'Sub-Inspector', 'Inspector']:
+                    return redirect('user_dashboard1')
+                elif rank in ['Superintendent', 'Director General']:
+                    return redirect('user_dashboard')
+                else:
+                    return redirect('home')
+            else:
+                messages.error(self.request, "Invalid login credentials")
+                return self.form_invalid(form)
+        except Users.DoesNotExist:
+            messages.error(self.request, "Invalid login credentials")
+            return self.form_invalid(form)
diff --git a/app/management/commands/hash_passwords.py b/app/management/commands/hash_passwords.py
new file mode 100644
--- /dev/null
+++ b/app/management/commands/hash_passwords.py
@@
+from django.core.management.base import BaseCommand
+from django.contrib.auth.hashers import make_password, identify_hasher
+from app.models import Users
+
+class Command(BaseCommand):
+    help = "Hash existing plaintext passwords in the Users table using PBKDF2."
+
+    def handle(self, *args, **options):
+        users = Users.objects.all()
+        updated = 0
+
+        for user in users:
+            try:
+                identify_hasher(user.password)
+                # already hashed
+            except Exception:
+                user.password = make_password(user.password)
+                user.save()
+                updated += 1
+
+        self.stdout.write(self.style.SUCCESS(f"Hashed {updated} plaintext password(s)."))
diff --git a/Crime_Report___Record_System/settings.py b/Crime_Report___Record_System/settings.py
--- a/Crime_Report___Record_System/settings.py
+++ b/Crime_Report___Record_System/settings.py
@@
 # Application definition
 INSTALLED_APPS = [
     'django.contrib.admin',
     'django.contrib.auth',
     'django.contrib.contenttypes',
     'django.contrib.sessions',
     'django.contrib.messages',
     'django.contrib.staticfiles',
     'app',
 ]
+
+# Custom authentication backend
+AUTHENTICATION_BACKENDS = ['app.backends.CustomAuthBackend']
